import Loader from "@/components/ui/global/Loader";
import ProfilePageSkeleton from '../../components/ui/global/profile/ProfilePageSkeleton';

const Loading = () => {
  return <ProfilePageSkeleton />;
};

export default Loading;
